- module: nginx
  # Access logs
  access:
    enabled: {{ filebeat.nginx.access_logs.enabled }}

    # Set custom paths for the log files. If left empty,
    # Filebeat will choose the paths depending on your OS.
    #var.paths:

  # Error logs
  error:
    enabled: {{ filebeat.nginx.error_logs.enabled }}

    # Set custom paths for the log files. If left empty,
    # Filebeat will choose the paths depending on your OS.
    #var.paths:
